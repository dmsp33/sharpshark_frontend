/**
 * Use dateUTC instead of date pipe if you want to see "UTC +0" or not,
 * otherwise use date pipe.
 */
export const DateFormats = {
    Simple: 'dd MMM yyyy',
    Extended: 'dd MMM yyyy, HH:mm',
};
